# 10 — Roadmap and task board

Phases are ordered so that something runs end to end as early as possible (decision D21) and each later phase widens one axis: rules coverage, card coverage, agent strength, or operability. Effort is given in rough "focused sessions" (a session ≈ a few hours of AI-assisted work) and is deliberately conservative; the rules engine and parser are where estimates are least reliable.

Status legend: `[ ]` todo · `[~]` in progress · `[x]` done.

## Phase 0 — Repository and scaffolding (≈ 2 sessions)

- [x] 0.1 `git init` in the project folder; create the GitHub repo `mtg-1v1-agents`; push; branch protection on `main`. *(The repository is public, and `main` is protected by an active ruleset: a pull request is required before merging (zero approvals, since one maintainer cannot approve their own), all three CI jobs — lint/typecheck/test/build, benchmarks, Docker — must pass, deletions are restricted and force pushes blocked. The bypass list is empty, so the rules bind the owner too: `main` only moves through a green pull request. Required checks are matched **by job name**, so renaming a job in `ci.yml` silently stops gating the branch — rename the required check with it.)*
- [x] 0.2 pnpm monorepo: `packages/{shared,engine,cards,agents,sim}`, `apps/{server,web}`, shared tsconfig, Biome, Vitest, tsup, Vite. *(Dependency versions are pinned centrally in the pnpm catalog in `pnpm-workspace.yaml`. Internal packages export TypeScript source and are bundled into the server by tsup, so there is no build ordering to manage.)*
- [x] 0.3 GitHub Actions `ci.yml` (lint, typecheck, test, build), plus a job that builds the Docker image.
- [x] 0.4 `scripts/fetch-scryfall.ts`: streams the gzipped `oracle_cards` JSONL to `data/scryfall/cards.jsonl`, skips non-card layouts, writes `meta.json` and prints counts. See ADR 0001.
- [x] 0.5 Dockerfile + docker-compose with a `data` volume; `pnpm dev` runs server + web with HMR and an `/api` proxy.

## Phase 1 — Engine core (≈ 10–14 sessions)

- [x] 1.1 State model, object ids, zones, seeded RNG, structural-sharing update helper, event emitter. *(Zone and state shape settled in ADR 0002. `GameState` gains its per-subsystem fields in 1.4/1.6/1.8/1.9.)*
- [x] 1.2 Turn structure with all steps; untap/draw/cleanup; land drops; turn cap. *(Also extra turns. The first-strike damage step is skipped until combat exists in 1.6. Cleanup's discard-to-hand-size is a real player choice, so it takes an injected `chooseDiscards` hook for now and fails loudly without one; 1.4 replaces the hook with a `pendingDecision`. Timing and land-drop limits are enforced; "is this card actually a land" waits on card definitions in 2.1.)*
- [x] 1.3 Mana: pool, costs (incl. hybrid/phyrexian/X), payment solver, mana abilities of basic and simple nonbasic lands. *(Pool reshaped to carry snow and spend restrictions per unit, and phyrexian mana folded into hybrid; see ADR 0003. The solver is exact, including {S} and {X}. Spend restrictions are carried but enforced in 1.4, when the engine knows what is being cast. `ManaAbility` is modelled and resolves without the stack; wiring real lands' abilities waits on card scripts in 2.1.)*
- [x] 1.4 Priority, stack, casting/activating, resolution, countering, fizzling, split second. *(The `step(state, decision)` model from docs/01 is live: `pendingDecision`, `applyDecision`, and the priority loop, which also retires 1.2's `chooseDiscards` hook. Stack resolution is LIFO, with countering and split second. Two parts wait on their own phases: a priority decision only offers `pass` until `legalActions` enumerates the rest in 1.5, and fizzling needs targets, also 1.5. Checking state-based actions and putting triggers on the stack sit immediately before priority (CR 117.5) and arrive in 1.7 and 1.8; abilities as objects on the stack need card scripts in 2.1.)*
- [x] 1.5 Targeting and legality (`legalActions`), hexproof/shroud/protection/ward. *(Shroud stops everyone, hexproof only opponents, protection is colour-based, and ward is reported as a cost rather than a targeting restriction. Fizzling (CR 608.2b) lands here too, as 1.4 flagged. Keywords are stored on the object for now and become the per-object cache `characteristics()` fills in 1.9. `legalActions` covers timing and payability — including mana untapped lands could still make — and reads what a card *is* through a `CardInfoSource` that 2.1 implements over card scripts; wiring it into the priority decision's options needs those same definitions.)*
- [x] 1.6 Combat with all evergreen keywords, damage assignment, first-strike step. *(Attack and block declaration with flying/reach/menace/vigilance/haste/defender, damage-assignment order, first and double strike, trample, deathtouch, lifelink. Damage is applied as one batch so creatures trade. Creatures die in 1.7, when state-based actions arrive; indestructible is carried for it. Damage assignment picks the canonical legal split (lethal down the order, excess to the last blocker or trampled over) — a free-form `assignDamage` decision is a refinement, not a correctness fix. Power/toughness joins keywords as stored-for-now, computed in 1.9.)*
- [x] 1.7 State-based actions, legend rule, counters, tokens. *(CR 704: losing at zero life, to decking and to poison; lethal damage, zero toughness and deathtouch, with indestructible sparing the first two but not zero toughness; +1/+1 and -1/-1 annihilation; tokens ceasing to exist; auras dying and equipment falling off; planeswalker loyalty. The legend rule asks the controller which to keep rather than choosing for them. Checked whenever a player would receive priority and repeated until nothing applies. `characteristics.ts` applies counters to power and toughness — the first slice of the 1.9 layer system, which subsumes it.)*
- [x] 1.8 Triggered abilities (APNAP, intervening-if, LKI, delayed), static abilities. *(Triggers fire on their event, wait, and go on the stack before the next priority in APNAP order, with the controller choosing among their own. Intervening-if is checked as it would go on the stack; last known information is captured before the source leaves, so dies triggers work. Delayed and once-each-turn triggers included. Abilities on the stack cease to exist on resolution rather than going to a graveyard. **Static abilities are not here**: they are continuous effects, and they only mean anything once the layer system exists, so they move to 1.9.)*
- [x] 1.9 Continuous effects and the layer system with timestamps and dependency. *(Layers 1-7 with all five of layer 7's sublayers, timestamp order within a layer, dependency ordering per CR 613.8 with a timestamp fallback on cycles, durations including until-end-of-turn and while-source-on-battlefield, and per-state memoisation. Every characteristic reader in the engine goes through `characteristics(state, id)`; the values on `GameObject` are printed values only. Dependency detection covers "changes what an effect applies to", which is complete for the current effect vocabulary because no change reads the board — `dependsOn` is the one place to widen when 2.1 adds one that does. The named cards in docs/09 (Humility + Opalescence, Blood Moon + Urza lands, Painter's Servant) are tested by shape here and by name once card scripts exist. Static abilities moved here from 1.8 and are continuous effects with a `whileSourceOnBattlefield` duration.)*

- [x] 1.10 Replacement and prevention effects. *(CR 614-616. The engine no longer changes state directly: it builds `RulesEvent`s and hands them to `runBatch`, which runs each past every applicable replacement before `performEvents` applies what survives — see ADR 0004. Self-replacement first (616.1a), never twice to the same event (614.5), and the affected player chooses when several still apply, which is a real `chooseReplacement` decision and so can pause a batch mid-flight. Prevention (615) is a species of replacement, with shields that shrink by what they absorb; regeneration (701.15) is a one-use shield over destruction specifically, which is why zero toughness — being put into the graveyard rather than destroyed — is not saved by one. Combat damage, drawing, discarding, dying, the legend rule, spell resolution, fizzling and countering all go through the pipeline, so lifelink's life gain is itself replaceable. The effect vocabulary is what the current engine can produce; card scripts in 2.1 add to it, and `performEvents` is where the docs/03 effect ops hang.)*
- [x] 1.11 Planeswalkers. *(CR 306 and 606. A planeswalker enters with loyalty counters equal to its printed loyalty, seeded into 1.10's `entersBattlefield` event rather than set afterwards — which is what lets a Doubling Season double it, since that is a replacement effect. Damage dealt to one removes that many loyalty counters instead of being marked (CR 306.8); a permanent that is both a creature and a planeswalker gets both. Attackers choose a defender per attacker from the defending player and the planeswalkers they control (CR 508.1a), and the declaration is validated against that list. Trample over blockers already went to "the player or planeswalker it's attacking" (CR 702.19b), so that needed a test rather than a change; damage aimed at a player is not redirected, the 2018 rules change. Loyalty abilities are activated at sorcery speed, once per permanent per turn (CR 606.3), with the counters paid as a cost — so Doubling Season does **not** double a `+2`, and an ultimate that empties a planeswalker kills it to a state-based action while its ability is still on the stack and resolves anyway. `legalActions` offers them and `whyNotActivateLoyalty` is the single legality check both it and activation use, so the engine never rejects what it offered. What each ability *does* is a card script, roadmap 2.1.)*
- [x] 1.12 London mulligan; game end conditions; loop detection. *(`setUpGame` shuffles, deals seven each and runs the mulligan rounds: both players declare in turn order before any mulligan is taken, those who mulligan redraw a fresh seven together, and on keeping a player puts one card under their library per mulligan taken (CR 103.4b) — the London mulligan is not "draw one fewer". `startGame` still skips all of it for tests and for 1.13's scenario builder. `game-end.ts` is now the only thing that writes `state.result`: conceding, effects that make a player win or lose outright, the turn cap, and the two draws we impose ourselves. Loop detection (CR 726) hashes a canonical projection of the state at every decision point — a repeat within a turn is a draw — and a decision cap backstops the loops the hash cannot see, such as one that shuffles. The hash is 53 bits across two FNV passes rather than 32, because a collision here is a game silently called a draw rather than a crash.)*
- [x] 1.13 Scenario builder for tests; unit suites for 1.2–1.12; invariant fuzzer with the `random` agent. *(All three live behind `@mtg/engine/testing`, a second entry point, so the runtime bundle keeps its zero dependencies and its size. The scenario builder has the fluent shape docs/09 sketches but describes permanents by what the engine can read off them rather than by card name — names arrive with card scripts in 2.1, and `name` is a label until then, which the legend rule happens to read. The `random` agent answers every decision kind uniformly from an injected generator, so a failing game replays from its seed. The fuzzer asserts the docs/09 invariants after **every** decision and checks `replay(decisions) == state`; the stronger `replay(log) == state` needs an event-log consumer that does not exist yet. The unit suites were audited against the eleven bullets in docs/09 rather than padded: they were already substantively there. Two of those bullets are blocked rather than missing — "changing targets" and additional/alternative costs need card effects in 2.1.)*
- [x] 1.14 Benchmarks; target ≤ 5 ms/game with the random agent. *(`packages/engine/bench`, run with `pnpm bench`: fixed-seed games in four cases — a baseline board, a wide board, a long game, and the baseline without loop detection so its cost is measured rather than guessed — reporting ms/game, games/sec and decisions/sec. **Median 2.2 ms a game**, inside the target. It did not start there: the first run was 23 ms, nine tenths of it CR 726 loop detection hashing a full position at every one of ~510 decision points. Chasing that found a rules bug the unit tests had not — the projection ignored `state.combat`, so ordering blockers on one attacker looked like a position repeating and **36% of wide-board games were ending as false draws**. Both halves are fixed, with tests and ADR 0005: the hash covers combat, the paused replacement batch and the triggers fired this turn, and it only watches a turn once that turn has run longer than any ordinary turn, since a loop never stops being one. CI runs the benchmarks and fails on a regression greater than 20% against the last run on `main`. `packages/agents/bench` and the ≤ 50 ms search-AI figure wait on agents in phase 4; a benchmark over real games waits on cards in 2.1.)*

## Phase 2 — Cards: schema, bootstrap set, resolver (≈ 5–7 sessions)

- [x] 2.1 Card-script zod schema + JSON Schema export; loader that turns a script into an engine `CardDefinition`; effect-op registry in the engine (first ~40 ops). *(The engine gained a card vocabulary — filters, quantities, selectors and conditions, all of them data — and **39 effect ops**, each implemented once and each proposing `RulesEvent`s so replacement effects apply without either side knowing about the other. Definitions live in `GameState` and objects take their printed characteristics and abilities from them; static and replacement abilities are *derived* from what is on the battlefield rather than registered and unregistered, so a blink or a control change needs no special case (ADR 0006). Casting is now the rest of CR 601.2 — timing, targets checked against what the ability asks for, and paying, with sources tapped in object order when the pool is short — and resolution runs the card's effects before the stack finishes the move. `@mtg/cards` has the zod schema for the friendly YAML forms docs/03 shows, the JSON Schema export (`pnpm cards:schema`, checked in CI), and a loader that converts and checks: every op exists, has the arguments it needs and none it does not, every `$target` was declared, and every `when`/`affects`/`change` names something the engine really has — all against the engine's own exported lists, so the two cannot drift. **Deliberately left:** ops that need a player choice mid-resolution (search, scry, may, modal, a chosen discard) need the effect list to pause and resume as a replacement batch does; until that exists a batch that pauses mid-effects throws rather than dropping the rest. Dynamic P/T (`*`/`*+1`) needs quantity-valued layer changes. Two bugs the first end-to-end test found are fixed here: triggers waited a full priority round because resolution granted priority directly instead of returning to `advanceToDecision` (CR 117.5), and the auto-tapper logged taps it discarded while trying a dual land's modes. A module cycle between `targeting` and `characteristics` broke real entry points depending on load order, and the keyword set moved to a module of its own to break it.)*
- [x] 2.2 Validator: schema, characteristic agreement with Scryfall, text coverage, executability smoke. *(`validateScript(script, card)` returns supported / partial / unsupported with the reasons and the validator version, which is what lets 2.4 cache a verdict and still re-earn it when the validator changes. Characteristic agreement is exact and makes a mismatch *unsupported* rather than a warning — a script that gets a cost wrong is a card quietly better than the one everyone else is playing with. `covers:` moved onto each ability, where docs/03 always had it, and coverage is checked per sentence with reminder text dropped: claimed twice is an error, claimed by nobody is partial. The smoke test plays the card in three synthetic games (empty board, a 2/2 opposite, the opponent's turn) with enough any-colour lands that cost is never the obstacle, and requires the engine to come out in a legal, answerable state without throwing; a scenario with no legal target is skipped rather than failed, because having no target is a normal fact about Magic. The Scryfall projection type moved from the fetch script into `@mtg/cards`, so the writer and the validator cannot read different shapes. **Deliberately left:** the per-hand-script test suite docs/09 wants (`packages/cards/test/scripts.test.ts`) needs hand scripts, which is 2.3; activated and loyalty abilities are not smoke-tested yet, only casting is; and a printed `*` power is reported as a disagreement, since dynamic P/T waits on quantity-valued layer changes.)*
- [x] 2.3 Bootstrap hand scripts (~80 cards): basics, shocks/duals/fetches/checklands, one or two cards per effect op and per keyword, a few planeswalkers, a few layer-system cards (Blood Moon, Glorious Anthem, Humility) — chosen to exercise the engine, not to make a deck. *(**60 cards**, not 80, and the shortfall is the point: the missing ones are the cards the vocabulary cannot yet say, listed with their blocking phase in docs/03 rather than quietly dropped — fetchlands and tutors, shocklands, checklands, modal spells, auras, Blood Moon, Bad Moon. Between them the 60 use all seven ability kinds and 26 of the 39 ops. Three are **partial** and named as such: Wrath of God, Swords to Plowshares and Turn to Frog each do less than their card says. `pnpm cards:new` fetches a card, scaffolds the script from the printed characteristics and records the projection in a committed fixture, so `scripts.test.ts` validates all 60 offline in CI. Writing real cards found four engine gaps and fixed them: an activation cost could not sacrifice its source (CR 118.3), a death trigger could not say "another creature **you control**", a loyalty ability could not take targets, and the auto-tapper could not pay a cost with two of the same pip — it picked a mode that finished the cost rather than one that made progress, so `{1}{B}{B}` never got its second black. Also: a bare `~` is null in YAML, which the loader now says out loud.)*
- [x] 2.4 `ScriptResolver` (hand → cached auto → auto-scripter → validate) with the `card_scripts` cache and `unsupported_requests` logging. *(The chain in order, with two of its four links standing in for things that do not exist yet — and both are ports rather than stubs, so what replaces them changes nothing here. The auto-scripter is phase 3: without one the resolver has no third step and everything outside the bootstrap set is unsupported, which is a configuration rather than a hole. The cache and the log are `ScriptStore`, shaped field for field like docs/06's `card_scripts` and `unsupported_requests`; the in-memory implementation runs now and the SQLite one lands with persistence in phase 5. Every verdict is cached, **including unsupported** — re-running a parser and a smoke test for a card that was unplayable an hour ago is exactly what the cache is for — and a verdict is only believed when the version that produced it is the version running now, which is why the row stores it. Partial scripts are never played and are logged like any other card a run could not have, with the run and the context that asked for it, because docs/03 wants hand-scripting effort to follow demand.)*
- [x] 2.5 Scenario tests per bootstrap card; differential test harness. *(**65 scenario tests over all 60 cards**, declared in each card's own YAML under `tests:` and run by `card-tests.test.ts`, which also fails a card that has no test at all. The vocabulary is a board, one action and an expectation, every part of it optional with a sensible default, and the runner reports failures as sentences so one run names every card that broke rather than stopping at the first. Writing tests for real cards found three engine bugs, each fixed with a failing test first: **a land played from hand never entered** — `playLand` moved the object instead of proposing an event, so "this land enters tapped" had nothing to replace (ADR 0004); **a permanent's own "as it enters" replacement was invisible**, because replacements were derived from the battlefield and a card entering is not on it yet (CR 614.12, noted on ADR 0006); and **casting checked the number of targets but never what the spell asks for**, so "destroy target artifact" could be aimed at a creature and "counter target noncreature spell" at a creature spell (CR 601.2c). The last one immediately failed three of the new card tests, which had been written against the hole. The differential harness plays two scripts through the validator's three smoke boards and the card's own tests and compares the whole resulting position rather than the declared expectations, so an auto script that quietly also drew a card is caught by a test nobody wrote. **Deliberately left:** it has nothing real to compare until the auto-scripter exists in phase 3 — what runs today is the harness against deliberately altered copies of a script, plus every bootstrap card compared with itself. The seeded regression games of docs/09 §3 wait on decks and the match runner in phase 5.)*

## Phase 3 — Auto-scripter v1 (≈ 8–12 sessions, then continuous)

- [x] 3.1 Normaliser (name → `~`, reminder text, sentence split, keyword lines, Scryfall keyword cross-check). *(`normaliseCard(card)` in `packages/cards/src/auto/normalise.ts`: the card's own name becomes `~` — the full name first, then the short name a legendary card calls itself by, since replacing the short one first leaves "~ Beleren" behind — and modern self-reference ("this creature", "this land") becomes the same `~`, because it is the same rule printed two ways. Punctuation is settled here rather than in the grammar: a loyalty cost's minus sign is U+2212 and not a hyphen, quotes are typographic, and both dash characters appear. **The sentence numbering is now shared rather than reproduced**: `linesOf` gives each line the numbers `covers:` is checked against, and `sentencesOf` is defined as its flattening, so the auto-scripter's `covers:` and the validator's coverage check cannot disagree. Keyword lines are all-or-nothing — "Flying, protection from red" is an ability, not half a keyword line — and the Scryfall cross-check runs in one direction only, because its `keywords` array mixes keyword abilities with action words (Jace Beleren lists "Mill", which the engine has an op for; an Equipment lists "Equip", which it has not), so the rest come back as `otherKeywords` for 3.2 to judge. The printed-name table that was hidden in `checks.ts` is now one shared table read by both the validator and the normaliser, with a test that every grantable keyword has a printed name. Run over the 60-card fixture, it normalises all of them with no notes. **Deliberately left:** only the front face of a two-faced card is read, and it says so in `notes` rather than pretending; the short name is the one guess in here, and a card whose given name is an ordinary English word would have that word replaced.)*
- [x] 3.2 Sentence classifier (keyword / activated / triggered / static / spell / loyalty / unknown). *(`classifyCard(card)` in `packages/cards/src/auto/classify.ts`, over the lines the normaliser produces. **The categories are the engine's seven ability kinds** — spell, activated, triggered, static, mana, loyalty, replacement — plus `keyword` and `unknown`, rather than the seven docs/03 sketched before the engine had card definitions: `mana` and `replacement` are missing from that list and the engine treats both as their own thing, so keeping it would mean the emitter reads the same line a second time to find out what it really was, and a second reader is where the two disagree (**ADR 0007**; a test asserts the correspondence, so a new ability kind cannot be added without a category). Most of it is the Comprehensive Rules rather than heuristics — CR 603.1 for trigger words, CR 602.1 for cost-colon-effect, CR 605.1a for which of those are mana abilities, CR 606.1 for a loyalty cost, CR 614 for replacement templating, and CR 112.3 for the residual, which makes spell-versus-static a rule rather than a guess. A cost is read all or nothing (CR 601.2h), because a cost read wrongly is a card cheaper than the one printed. The ground truth for the tests is the bootstrap set itself: every sentence a hand script claims, classified and compared against the `kind:` a person wrote, with the validator deciding which sentences are unclaimed because their script is partial. It agrees on all sixty and leaves no line unknown. **Deliberately left:** the residual rule is only as good as the rules above it — a replacement template nobody has taught it is called static rather than unknown, and the differential harness is what would catch that on a card that also has a hand script. Sabotage-checking found a rule with no test behind it — that half a cost is not a cost — which is now tested.)*
- [x] 3.3 PEG grammar v1: costs, targets, quantities, durations, conditions, common effect verbs; anaphora resolution. *(A PEG's shape — ordered choice, backtracking, a rule that either matches or puts the cursor back — **written rather than generated with `peggy`**, because what a rule produces is a piece of script built from a closed vocabulary and that is worth type-checking, and because a rule that is a function can be tested and sabotage-checked on its own (**ADR 0008**). `reader.ts` is the machinery, `phrases.ts` the shared vocabulary (quantities, players, filters, durations, the object phrases), `verbs.ts` the ~20 effect templates, `parse.ts` the entry points — effects, triggers, mana modes, statics, replacements and activation costs. **44 of the bootstrap set's 46 ability lines parse**, and every one of them is loaded through `loadCardScript` in a test, because a parse that produces a script the engine cannot read is worse than one that admits it failed. The two that do not are Humility and Turn to Frog, which want layer changes the script vocabulary cannot say. The unit is the sentence, so a parse can stop at a sentence boundary and still be worth having — the shape of a partial script — but never mid-sentence, because half a sentence is a different card. Sabotage-checking eleven rules found two with nothing behind them: that half a cost is not a cost (`{1}, Discard a card` was read as `{1}`, a card cheaper than the printed one) and, earlier, the binding rollback — without it Giant Growth declared three targets for its one "target creature" and Time Warp seven, because each verb that tried and failed left its declaration behind. **Deliberately left:** "its controller" has no spelling in the script vocabulary, so Swords to Plowshares stops after its first sentence; a leading "Until end of turn," is not read; and the order of the verb list is part of the grammar with nothing enforcing it, which the golden corpus in 3.4 is what properly catches.)*
- [x] 3.4 Emitter + golden corpus (≈ 300 cards spanning the common templates). *(`emitScript(card)` assembles a whole script — characteristics off the printed card, keywords from the normaliser, one ability per classified line — and `autoScripter` makes it **the resolver's third link**, which 2.4 built as a port with nothing behind it. The corpus is **370 real cards** (two core sets, `pnpm cards:corpus`, committed because CI has no network) and `corpus-goldens.json` is what the auto-scripter makes of each, rewritten by `pnpm cards:goldens` and checked by `pnpm check`. It reads **124 supported, 244 partial, 2 unsupported**: the two are the cards with a printed `*` power, and **no card in the corpus is emitted with a characteristic that disagrees with the printed one or makes the engine fall over** — a test asserts both, because a script that does less than the card says is never played while one that says something different would be. The `covers:` rule is the whole safety story: it claims only the sentences the grammar actually read. **The differential harness from 2.5 finally has something to compare** — 56 of the 60 bootstrap cards have both a hand script and a supported auto script, and all 56 agree. It earned its keep on the first run: a card whose spell text runs to two lines was emitted as two spell abilities and the engine resolves only the first, so Twisted Image switched power and toughness and never drew its card (CR 112.3a). The loader now refuses a second spell ability, and refuses a filter object with nothing in it, which used to mean "any permanent" silently — a double-parsed script came out targeting anything at all. **Deliberately left:** the smoke test proves a supported card is legal, not that it is right; only the 56 with hand scripts are checked for that, and the rest rest on the grammar's rules being what they say. `pnpm cards:coverage` over the whole of Scryfall is 3.5.)*
- [x] 3.5 `pnpm cards:coverage` full-Scryfall report with top failing patterns; nightly workflow. *(The whole pipeline over every card Scryfall has, streamed a line at a time, writing `reports/coverage.json` for machines and `reports/coverage.md` for people. **Measured over 34,733 cards: 3,398 supported, 30,333 partial, 884 unsupported, 118 unscripted — 9.8% of all cards, 8.0% of the ones that have rules text, and 13,530 of 76,032 sentences claimed.** That is a long way from 3.6's 25% target, which is what 3.6 is for; the number is reported rather than framed. The score is deliberately not one number: a card with no rules text is supported without anything being read, so those 1,253 are counted apart, and sentences-claimed is reported alongside because a card is partial the moment one sentence is unread. **The useful half is the pattern table** — each unread sentence generalised (numbers and mana symbols are what differ between printings of one template) down to its opening words, ranked by how many sentences share its shape, with an example card. The top of it is a to-do list for 3.6: `enchant creature` (893), `at the beginning of your upkeep` (728), `flash` (627), `equip {M}` (592), `whenever ~ deals combat damage to` (466), `activate only as a sorcery` (415), `choose N —` (393). `nightly.yml` runs it against the day's data and keeps one issue up to date with the table, updated in place rather than commented on, and runs the long fuzz beside it — which needed a budget knob, since the fuzzer played forty games whatever the context and a nightly fuzz job would have run exactly what CI already runs. Neither gates anything. **Running it over every card immediately earned its keep**: the smoke test sizes its board from the card's mana value so that cost is never what stops it, and Gleemax costs `{1000000}`, so the validator set about building a million lands three times and never came back. The board is now capped at twenty and a card that wants more is skipped with a reason — a bug a 370-card corpus of two core sets was never going to find. **Deliberately left:** a pattern is a prefix, so it cannot tell "destroy target creature" from the same template with a qualifier past the sixth word, which is the right trade while the top of the list is this coarse.)*
- [ ] 3.6 Coverage push to ≥ 25% of Scryfall `supported` (vanilla/French-vanilla creatures, burn, pump, simple removal, cantrips, counters, ETB/dies triggers, simple anthems, basic mana rocks/dorks). *(**Not met, and left unticked rather than reframed: 3,793 of 34,733 cards — 10.9%, against a 25% target.** Up from 3,398 (9.8%), with sentences claimed 13,511 → 15,063 of 76,032. The corpus goldens read 138 supported where 3.4 read 124. **The ranking was measuring the wrong thing**, which is the more useful half of what landed: a pattern's `count` is how many sentences share its shape, and working from it puts `enchant creature` at the top with 893 — a template that on its own would finish **zero** cards, because every Aura also prints "enchanted creature gets …" and the engine has no selector for what an Aura is attached to. Each pattern now carries **`finishes`**: how many cards have nothing else left unread, so teaching it is the last obstacle. That column is what `reports/coverage.md` tells a reader to work from, and 3.5's fallout split is what makes it trustworthy. Six templates taught off it, each sabotage-checked: **flash and split second** off a keyword line (fields on the card, not entries in `keywords:`, and the validator lets the field claim the line only when the script sets it); **the filter's adjectives and qualifiers** — "attacking or blocking", "tapped", "untapped", "white or blue", "with flying", "without first strike" — which is the broadest of the six because a filter appears in removal, pump, anthems and wipes alike, and every predicate behind it was already in the engine (CR 115.1); **a second damage clause**, "6 damage to target creature and 2 damage to target player" and "1 damage to each creature and each player", which was the commonest thing between a burn spell and being read; **"add one mana of any color"** as the five colours in five modes (CR 105.1 — colourless is not one); **"~ enters with four +1/+1 counters on it"** as the self-replacement it is (CR 614.1c); and **"activate only as a sorcery"** as the `sorceryOnly` an activated ability already has. Teaching the step triggers **whose** step they are found a latent hole: the engine fires one on `whose === 'any' || controller === activePlayer`, so a script that left `whose` out got "your upkeep" by accident and the type requiring it never saw the object — an "each player's upkeep" ability would have triggered half as often as the card printed. The loader now refuses a step trigger without it. **What stands between 10.9% and 25% is named rather than half-done, and it is mostly engine work**: an `attachedTo` effect selector for Auras and Equipment (the four largest shapes, ~2,400 sentences, and 0 `finishes` between them until it exists), a combat-damage trigger (466), modal spells and the mid-resolution choice that search, scry and "you may" also wait on (393), additional casting costs (317) and cost reduction (224), and the cost-carrying keywords — cycling (305), kicker (202), crew (190), flashback (189), morph (141), ward (93), equip (592). "Activate only once each turn" is the smallest of them: a triggered ability has `onceEachTurn` and an activated one does not. The largest remaining grammar bucket, `at the beginning of your upkeep` at 279 `finishes`, is **not one template**: the trigger reads, and the bodies are 279 different effects. Phase 3 is "then continuous" and phase 7 widens by the coverage page, so this is where the rest of it lives.)*

## Phase 4 — Agents (≈ 6–8 sessions)

- [x] 4.1 `PlayerView` and the import-boundary test. *(`viewFor(state, player)` projects what one player knows, and it is the only place in the system that decides that. **The hiding is in the types**: the opponent's side has no `hand` field to read rather than one that happens to be empty, a view is plain data with no back-reference to the state it came from, and **neither library is in the view at all** — nobody knows a library's order, not even its owner (CR 401.2), and a view that showed A their own would let an agent play as though every draw were decided. Objects come through `characteristics()`, because an evaluator judging a board from printed power would misread every anthem in Magic. **`@mtg/engine/view`** is a separate entry point carrying the view, the decision vocabulary and the RNG interface, and nothing whose runtime import graph reaches `state/game-state.ts`; `packages/agents` imports that and never `@mtg/engine`, and `import-boundary.test.ts` checks both halves — the second is what stops the first from being a naming convention. **ADR 0009** records the departure from docs/04, which wrote the interface as `decide(state, decision, view, rng)`: the state was the first parameter of a method whose whole purpose was to withhold it. The cost is named rather than hidden — 4.3's search cannot call `step` on the true state and has to work from a determinisation built out of the view, which is the same discipline one level down. **The sabotage checks earned their keep twice.** Ten tests that each named a field and asserted it was absent *all stayed green* when a new field carrying the opponent's hand was added to the projection: the failure mode of a hiding rule is a leak nobody thought to look for, so a list of places to look cannot catch it. The replacement states the definition instead — two states differing only in what is hidden must project to the same view — and the first version of *that* compared boards whose hidden cards had different names, which an id leak walked past because both boards numbered their objects the same way. The version that landed renumbers as well as renames, and all three leaks now fail it. **Deliberately left:** revealed cards, which docs/04 listed and the engine does not track — nothing records that a card was revealed or scryed, so there is nothing to project, and it is named rather than faked. The boundary is checked by reading import specifiers, so a computed dynamic import would slip past it; ADR 0009 says why that trade is right. `packages/agents` keeps its production code environment-free (`types: []`) with a second tsconfig for the tests, since the boundary test's job is to read source files.)*
- [x] 4.2 Priority offers real actions, and the engine pays for it: back under the 5 ms budget. *(**Met at 4.8, on the code as built: a median baseline game in 3.9 ms, a long game in 4.2 ms, wide combat in 2.3 ms.** Most of the gap below was the measurement, not the engine: every number here was taken under `tsx`, whose `keepNames` transform costs a call each time a closure is made and read about a fifth slower than the code tsup builds. The engine exactly as this note left it, built and timed on the same games, plays the baseline in 4.0–4.3 ms; 4.8's own engine work — static replacements remembered per board, `flatMap` out of the loops that run on every event — took the rest. The benchmarks now time the built code (ADR 0013). What follows is the note as written at 4.2, when the number read 5.2 ms. — **Not met at the time: a median baseline game is 5.2 ms against a 5 ms target, and a long game 5.4 ms.** Left unticked rather than reframed, even though the wiring itself is finished and the number came down by four fifths on the way. Roadmap 1.5 built `legalActions` and left it disconnected, because deciding what a card *is* needed the definitions that arrived in 2.1; until it was connected the only option on a priority decision was `pass`, so **the fuzzer had never cast a spell and no agent could have**. `priority.ts` now grants priority and computes the options from the state that results, a cast action carries the targets that casting will choose (CR 601.2c) with a cap of 24 combinations per spell, and the fuzz deck has lands, creatures, a trigger, a burn spell and a removal spell in it. Connecting it found **a real loop-detection bug**: `applyPriority` ran the game forward after a resolution and `applyDecision` then ran it forward again, so every post-resolution position was hashed twice and CR 726 called the game a draw. Nothing could reach it until a fuzz game could put a spell on the stack. **The cost was 46 ms a game**, against 2.5 ms for the turn loop that cast nothing — not a regression so much as the first honest measurement, since the old number was of a game in which nothing happened. Getting it to 5.2 took eleven profiled changes, of which **six were measured and thrown away** for not earning their place, including one that made it twice as slow. The large one is **ADR 0010**: the object table was a `Map` rehashed in full on every write — one point of damage rehashed every object in the game — and is now an array indexed by object id that copies as a block, which was a fifth of the engine's time and 8.1 ms to 5.9 ms on its own. The rest are smaller and named in the commits: a settled board is remembered so a sweep that would find nothing does not run, a board with no continuous effect on it skips the layer walk, the loyalty walk asks the step before it walks anything, and `CardInfo` is derived once per definition rather than per card per grant. **The benchmark harness was wrong in two ways that flattered it**, both fixed here: cases were seeded from their own names, so `no-loop-detection` priced two hundred *different* games rather than the setting and reported 19% for a check that never ran; and cases ran one after another, so whichever went first paid for a cold process. Cases now interleave after a shared warm-up, and a case that prices a setting plays the same seeds as the case it is compared with — the report shows it, since the two now agree on decisions per game. **Deliberately left:** the static evaluator and the `greedy` level, which moved to 4.3 when this phase was spent on the engine instead. The remaining 4% is not in one place — state-based actions, priority grants, leaving a step and casting are each about a seventh of a game, and object spreads about an eighth — so closing it is another representation change rather than another micro-optimisation; **4.8** re-measures both budgets together. Two of the sabotage checks could not fail, and that is the honest result rather than a gap: a short cut that is *looser* than the rule behind it is slower, not wrong, because the rule still filters what it offers. Only tightening one is a bug, and the tests catch that.)*
- [x] 4.3 Static evaluator with weights file; `greedy` level; determinised depth-2 search with beam for main-phase sequencing; per-decision budget; `search` and `deep` levels. *(**All four levels play whole games, and the ladder holds at 500 games a rung: greedy over random 431–29, search over greedy 273–194, both far past p < 0.001.** **The evaluator** (`packages/agents/src/evaluate.ts`) scores a view for its viewer, term by term, with weights in a JSON file checked against the `Weights` type at compile time and by `parseWeights` at run time; where docs/04 asked for a term the view cannot show, the term is left out and docs/04 lists which. **Greedy has no simulator** and prices what it cannot see with explicit priors (**ADR 0011**). **The search has one, and how it gets it is ADR 0012**: ADR 0009 said the search would build a state from the view, and the view turned out to lack what the rules need — spell targets, "until end of turn" effects, timestamps, triggers — so the engine instead determinises the *true* state, replacing every card the player cannot see (the opponent's hand drawn from cards they have shown, both libraries unknown cards), cutting definitions to what they can see, and swapping the generator. The driver hands the agent a `Simulator` for it with every decision; `World` is opaque and the agents package still imports nothing but `@mtg/engine/view`. It is safe by the view's own property, tested the view's own way: swap every hidden card and require the same world — and the shared fixture was widened to vary the generator and loop hashes when a sabotage check showed only narrower tests caught those. **The search** plays each candidate to a horizon — stack empty, step changed — in two sampled worlds; depth-2 with a beam (the opponent's worst reply is assumed, its own follow-ups sequenced); **a budget of engine steps rather than milliseconds**, so a game stays a function of its seed, costing 3.6 ms a decision on average and 15.2 ms at p99 against docs/04's 20 ms aim. **Watching it disagree with greedy found three bugs**, each with a test now: the evaluator scored a spare land at 1.5 in hand and 0.1 in play, so neither agent played its ninth land; a candidate cut short by the budget was compared with ones that finished, which favoured passing because passing is searched first; and a tie at depth — common, because the opponent's worst reply differs by line and costs the same — also went to passing. Candidates are now compared only at a depth all of them reached, and ties go to the one-step look. **The top rung is not in the unit suite**, and that is a measurement rather than a shortcut: search wins about three decided games in five against greedy, and two seed sets of a hundred gave 48–28 and 32–35, so `pnpm ladder` plays docs/09's 500 nightly and fails the job if a rung does not hold. The unit suite pins the search's behaviour instead — burn aimed at the creature it kills where greedy aims at the biggest, replies searched, follow-ups sequenced, the budget never exceeded — and all 21 of its rules and the determiniser's were sabotage-checked. **Deliberately left:** combat decisions are greedy's for every level until the combat solver (**4.4**), and that is most of what decides a fuzz game — on boards dealt three creatures a side, search's edge is the same as on empty ones; the opponent's hand is guessed from what they have shown rather than from a decklist, and the player's own library is unknown cards, until the knowledge record of phase 5; the mulligan's projected turn-3 board; and the weight-tuning harness (**5.x**).)*
- [x] 4.4 Combat solver (attacks and blocks). *(**Search now beats greedy 290–137 over 500 games (p = 5 × 10⁻¹⁴), up from 273–194; switched off with everything else equal, 275–193 — so the solver alone moves search's share of decided games from 59% to 68%.** `packages/agents/src/combat/`: a **model** of combat damage worked out from the view, following the engine's `assignCombatDamage` rule for rule — first-strike step, damage-assignment order with the last blocker soaking up the rest, trample, deathtouch, lifelink, indestructible (CR 510, 702) — and a **solver** built on it, as docs/04 item 3 describes: blocks improved one attacker at a time with pairs for gang blocks, never a lone block on menace, a chump only when facing lethal; attacks drawn from a pruned set (none, all, each alone, the good ones together, all but one), each answered by the predicted blocks. The search checks the solver's best few against the engine in its worlds, and the opponent blocks with the solver there too, so what the model cannot see — prevention, triggers — is still caught. **The evaluator's threat term learned about blockers**: it used to ask only whether a side's creatures could deal the other's life total, and so could not see that attacking with everything leaves nothing home; now each blocker stops the biggest attacker it can reach, and which creatures count follows from whose turn it is (CR 502.3). The unit suite has the case greedy gets wrong — its only 1/1 attacks into a tapped board and loses to the 5/5 swinging back — and the solver stays home, or attacks anyway with vigilance. **Ten rules survived their first sabotage**, two of them only because the sabotage itself changed nothing; the other eight had no test behind them: "the last blocker soaks up the rest" is only visible through lifelink; "survivors are healed" was trivially true because the model never marks new damage on objects, so it needed damage from before combat; menace and flying in the *predicted* blocks were invisible because the real block path filters lone menace blocks anyway; the attack candidates beyond "each alone" needed boards where only they find the answer — flyers go and bears stay, or all but the one that must stay home; the combat horizon passed because "no attack" skips straight to the end of combat; and the in-world policy needed a board where greedy and the solver block differently. All 30 combat rules now fail a test when disabled. **Deliberately left:** greedy keeps its rules of thumb, as the ladder's baseline; attacking planeswalkers, and blocks that depend on what the model cannot see, are the engine's to check rather than the solver's to find; instants during combat are passed over in the search's combat lines; and the solver's draws — 73 in 500, from boards that keep their blockers home and stall into the turn cap — are the evaluator-tuning harness's to weigh (4.7).)*
- [x] 4.5 Mulligan and play/draw logic. *(**The engine gained the decision the rules call for**: CR 103.1 says a player *chooses* who plays first, and until now `playerOnPlay` was fixed when the state was made. With `chooser` given, `setUpGame` asks a `playOrDraw` decision before a card is shuffled or dealt, and the answer sets the player on the play — who declares a mulligan first (CR 103.4) and skips the first draw (CR 103.7a); the `gameStart` event says who chose. Without it, nothing changes. The fuzzer plays chosen-start games under its invariants, and all four agents answer the new kind. **The agents' side is docs/04 items 5 and 6** (`packages/agents/src/opening.ts`): play, unless a play/draw record shows the draw ahead by more than chance — twenty games a side and a one-sided two-proportion test at 5%, because being on the play is worth a whole turn and twenty games can show the draw ahead by luck; a mulligan by land count, an early play in the lands' colours, and a **projected turn-3 board** that must clear a threshold scaled to the hand size; and **London bottoming** that tries every kept subset and keeps the best-curved one. **Measured honestly, the new opening logic makes no difference on fuzz boards**: against the old rules, 234–223 and 201–181 over 500 games each (p = 0.32 and 0.17), and the threshold at 0, 1.5 or 2.5 moves nothing — every fuzz land makes every colour and every fuzz spell costs two or less, so nearly every hand clears every bar. The logic is docs/04's and is tested rule by rule; whether it earns its keep is a question for real decks. Two rules survived their first sabotage: the zero-spread guard in the play/draw test was dead code (with no spread the rates are equal and the ratio is NaN, which is greater than nothing) and was deleted, and moving the active player at the choice needed a board where the choice changes who that is. **Deliberately left:** where the play/draw record comes from — per-cycle statistics are phase 5's, and the match runner that names the loser of the last game as chooser is 5.1; the ladder boards still fix the starting player, so the ladder's numbers are unchanged; and whether the mulligan threshold is right is the tuning harness's (4.7), on decks that make hands differ.)*
- [x] 4.6 Sideboarding agent with tag priors and matchup stats. *(**Two halves, on either side of the boundary.** `@mtg/cards` reads **tags** off a script (`cardTags`): `is` — types, `spell`, and roles like burn, counterspell, lifegain, graveyard — and `vs` — what its removal-shaped ops can be pointed at, as their filters say (docs/03 "Tags"). The bootstrap cards are the ground truth: Doom Blade answers creatures, Naturalize artifacts and enchantments, Lightning Bolt creatures and planeswalkers, Prey Upon the creature it fights and not the one it fights with, Giant Growth nothing. The vocabulary lives in `@mtg/shared`, because the agent that reads it may not import `@mtg/cards` (ADR 0009). `@mtg/agents` has the **sideboarding agent** (`sideboard`), a pure function of plain data: a card with a record in the matchup scores its drawn-minus-not-drawn win rate shrunk toward the deck's by twenty games (docs/05); one without scores by the share of the opponent's seen cards its tags answer; one with neither is never swapped on a guess. Up to `maxSideboardSwaps` (new run setting, default 4) pairs beat a margin, land for land and spell for spell, castable in the colours the lands will make, never banned. `@mtg/sim` turns definitions into what the agent reads, and an end-to-end test on real scripts sides Naturalize in for Doom Blade against Icy Manipulators — and keeps Doom Blade against Ornithopters, which it answers just as well. **The colour check was wrong the first time**: it asked that the whole deck stay castable, so a deck with one card it could never cast could make no swap at all; it now asks that the incoming card be castable and that no card castable before be stranded. Every rule was sabotage-checked, and one test survived its sabotage because the uncastable card in its fixture was itself the first thing swapped out; the fixture now uses a card the agent cannot score. **Deliberately left:** applying the plan between games 2 and 3, storing it with the match and building each card's matchup record are the match runner's and the statistics aggregator's (**5.1**, **5.2**); the trial-batch version docs/04 names as a later improvement; and whether the prior's weight and margin are right is a question for real matches.)*
- [x] 4.7 Sanity-ladder tests (search > greedy > random); weight-tuning harness. *(**The ladder half landed with 4.3** — the bottom rung at 60 games in the unit suite, the whole ladder at 500 games a rung nightly — **and the harness is here** (`packages/sim/src/tuning.ts`, `pnpm tune`): `compareWeights` is docs/04's "evaluator A vs B for 1,000 games", every board played **twice with the seats swapped** so a board's lean toward one seat cancels within the pair (two identical sets split every pair exactly); `hillClimb` moves one term at a time — a factor for most, one for the land count and the danger threshold, never the value of a win — keeps a move only when it wins at a one-sided p below 5% on fresh seeds, never replays a move refused from the same weights, stops when nothing is left to try, and then **plays its result against its start on seeds no step was judged on**, because twenty proposals at 5% pass about one that is no better and the games that passed it are the games that flattered it. How each term may move is `weightTuning`, beside the weights in `@mtg/agents`. `parseWeights` now also refuses the sets a proposal could make that break an agent without a single non-finite number: a spare land worth as much in hand as in play, which is then never played, and a land count or life threshold that is not whole. **Measured honestly, the harness finds nothing to change on fuzz boards, and `weights/default.json` is unchanged**: 82 steps over three climbs (greedy at ±25% and at ×2, search at ±25%) kept none, and 60 of them split exactly even — the signature of a move that changes no decision. The boards explain it: no planeswalkers, no poison and a land that makes every colour leave four terms nothing to act on, greedy never changes a life total so the life terms cannot separate its options, and every fuzz hand clears any mulligan bar. The moves that did change decisions made the agent worse — `cardInHand` doubled lost 17–183 and `land` halved 19–179. The two questions earlier phases left for this one got answers: **the search's draws are not the threat term's doing** (4.4) — `lethalOnBoard` halved and doubled at `search` went 161–162 and 161–161 over 400 games, draws unchanged — and **the mulligan threshold cannot be tuned on these boards** (4.5). The harness's own tests use a set that hoards its hand, which the default beats 19–1, and all 19 of its rules fail a test when disabled; four of them came from running it for real — the search climb replayed one refused move three times, which is why refused moves are now remembered, cleared when the weights change, and a climb with none left says it has converged. **Deliberately left:** tuning on real decks, where the terms fuzz boards cannot reach would have something to say, is phase 5's; the harness runs on demand rather than nightly, since a climb that finds nothing on fuzz boards would only spend CI minutes proving it again; and per-matchup weights (docs/04's "coach") are later still.)*
- [ ] 4.8 Benchmarks; target ≤ 50 ms/game at `search`. *(**Not met: a median game with both players at `search` takes 83–87 ms against 50 ms**, down from 145–154 ms; one searcher against greedy takes 45 ms, and a searched decision 1.3 ms on average and 8 ms at the 99th percentile, against docs/04's 20 ms. Left unticked rather than reframed. **The benchmarks are here**: `pnpm bench:search` (`packages/sim/bench`, since the agents package cannot run a game) plays whole games on the ladder's boards in two cases, and CI runs it beside the engine's and compares both with `main`. **Every change to the search changed nothing it decides**: a fingerprint of thirty games' decisions held through all of them, and `search.test.ts` holds it with a `remember` setting — off and on give identical reports at four budgets over positions from real games. The search now remembers each line it works out within a decision, charging a remembered line the steps it took so the budget runs out where it always did; plays a repeated deal once, through a new `Simulator.sameDeal` (ADR 0012's amendment), since two samples are the same deal two decisions in five; the block solver remembers what it has projected; and views and evaluations allocate less. Three more changes were measured and thrown away. **Measuring it found the benchmarks were wrong**: `tsx` had put a fifth on every number since 1.14, which is why the engine benchmark, the ladder and the tuning harness now run on the code as built (ADR 0013) — and why 4.2 is ticked here. **Why the rest is not there**: what remains is spread thin — engine steps a third of a searched game, views and scoring a fifth, the combat solver a sixth, the garbage collector a tenth — and the one lever big enough is the search's own settings, which on fuzz boards buy almost nothing: the depth-2 search overturned the one-step look in 1 of 556 decisions, and one sample, a beam of one, one follow-up, no replies or half the budget each left every decision in ten games unchanged. Cutting the defaults on boards where they happen not to matter would be tuning to the benchmark; the unit suite has positions where depth decides, and real decks will have more. Of thirteen rules sabotage-checked, ten were caught first time; two survived and now have tests — the replacement cache following a reordered battlefield, and a view keeping the object table's order — and the third, storing only lines the budget never cut short, was dead code, since such a line could never be reused anyway, and was deleted. **Closes with:** real decks (phase 5), on which the tuning harness can weigh whether depth, samples and budget earn their cost, and the defaults can be set from that rather than from fuzz boards.)*

## Phase 5 — Simulation loop and persistence (≈ 5–7 sessions)

- [x] 5.1 Match runner (Bo3, play/draw rules, sideboarding hook), cycle runner, tie handling. *(`packages/sim/src`: **`deckBoard`** makes a game from two decks and refuses a card nobody has scripted; **`playMatch`** plays best of three with who plays first *chosen* as CR 103.1 says — game 1 by the player the cycle names, then by the loser of the previous game, or after a draw by whoever chose in it — a drawn game counting for neither side, and a drawn match when wins are level after three; between games 2 and 3, and only then, each player's sideboarding hook sees what the opponent has shown and returns the deck for game 3, and a plan that changes the seventy-five or the sixty is refused. **`runCycle`** plays `matchesPerCycle` matches with game 1's choice alternating, scores a drawn match as half, plays `tiebreakMatches` more on a tie and flips the cycle's seeded coin if it is still one, keeps each deck's play/draw record game by game and makes every match's agents knowing it, and sideboards with docs/04's agent by default. Every game keeps its seed and decisions, so it can be replayed. **Writing the tests found one bug**: the hook was handed the live list of games, which the match went on adding to, so a hook that kept its context saw history change under it; it gets a copy now. Of 27 rules sabotage-checked, one survived — what the opponent has *shown* was only checked to be their cards, which their library also is — and the test now bounds it below their whole deck. **Deliberately left:** the event logs the statistics come from, and per-card matchup records for sideboarding, are the aggregator's (5.2); the deck change after a cycle is the deck agent's (5.4) and legalisation 5.5's; persistence and resume are 5.6's; games run one after another, and running them across workers is 5.7's.)*
- [x] 5.2 Statistics aggregator from event logs (all stats in 05), decay roll-up, shrinkage. *(**Every game leaves an event log** in docs/06's format (`eventLogOf`), and `playGame` can record each decision scored by the evaluator on the decider's own view. **The aggregator** (`packages/sim/src/stats.ts`) reads a log alone — no engine, no replay — and returns counts for every statistic in docs/05: drawn and not drawn with their wins, cast, dead in hand, turn of first cast in the player's own turns, impact as the caster's evaluator swing across a resolution, mulligan blame, screw and flood over the games that reached turns four and eight, colour screw, play/draw, game length, and each card's record filed under the opponent's deck generation. **Counts, not rates, are stored** (`@mtg/shared`, so the deck agent can read them): they add across games, `rollUp` decays them across cycles with docs/05's half-life of three, and rates are read off them with the n0 = 20 shrinkage the sideboarding agent now shares. `runCycle` reads each game as it ends — a cycle's logs would be hundreds of megabytes, so none are kept unless `onGame` keeps them — and **sideboarding now reads each card's record** against the opponent's current deck from earlier cycles and this one. The aggregator is checked against the engine on real games — the cards it says ended in each hand are exactly the cards the final state has there — and rule by rule on hand-built logs. **Found on the way**: the event log's list of decision kinds had no `playOrDraw`, which 4.5 added to the engine; a compile-time check now keeps the two lists together. Of 34 rules sabotage-checked, two survived and have tests now: a land that left the battlefield, and screw at exactly three lands. **Deliberately left:** a land counts for its owner even if someone else controls it, and only lands count toward screw and flood, not other mana sources; impact is measured for spells, not activated or triggered abilities; storing counts and logs is 5.6's; the deck agent that reads them is 5.4's.)*
- [x] 5.3 Constrained-random seed-deck generator with re-roll on unsupported. *(**`generateSeedDeck`** (`packages/sim/src/seed-deck.ts`) rolls docs/05's seventy-five from any pool of Scryfall projections: one to three colours at 30/50/20 unless the run names them, `seedDeckLands` give or take the jitter, up to 40% of them nonbasic and the rest basics split evenly, sixty cards with at most four of a name and eight costing five or more, fifteen for the side from names the main did not take, copies weighted toward four for cheap cards. A card may be drawn only if it is not digital-only, is legal or restricted in the run's base format, is not banned by the run, and fits the colours; restricted — by the format or the run — means one copy in the 75, and basics are unlimited. **Every card drawn goes through the `ScriptResolver`**; one it cannot support is put back, never drawn again for that deck, listed with where it was drawn, and logged as an unsupported request tagged with the run. The same pool, settings, ban list and seed make the same deck whatever order the pool arrives in. A thin pool tops up the names it drew before giving up, and gives up with a `SeedDeckError` rather than a short deck. The run's ban list has a type now, `BanList` in `@mtg/shared`, for 5.5 to build on. **`pnpm seed:deck`** rolls one from the whole of Scryfall and prints the decklist: about half a second a deck with the smoke test, 120–300 draws put back at today's 10.9% coverage. Two seed decks drawn from the committed fixtures by the real resolver play a whole match in the tests. **Found on the way**: a snow-covered basic was being drawn as a *nonbasic* land — four Snow-Covered Wastes in a red-green deck on the first real roll — because only plain basics were kept out of that draw; any basic is kept out now, and any basic is unlimited (CR 205.4c, 100.2a). Of 37 rules sabotage-checked, three survived and have tests now: the 40% nonbasic cap (the test pool never had enough nonbasics to reach it), the eight-card curve cap (the test read the cap from the constant it was checking), and the section a re-roll is logged under. **Deliberately left:** basics split evenly rather than by the spells' coloured pips, which the deck agent's colour-screw diagnosis is for (5.4); a card whose own text allows any number of copies is held to four; both agents getting the deck as generation 0, and where the run's seed for it comes from, are 5.6's; the ban list's audit trail and legalisation are 5.5's.)*
- [x] 5.4 `StatisticalDeckAgent`: diagnosis, Scryfall shortlist query, static quality model, on-demand scripting via the scripting worker, optional trial batches, reason text. *(**`StatisticalDeckAgent`** (`packages/agents/src/deck-agent.ts`) is a plain function of data behind two ports, like the sideboarding agent, and never sees a definition (ADR 0009). **Diagnosis**: screw trades the worst spell for a land, flood a land for a spell — never the last land making a needed colour — and colour screw trades the most over-supplied land for one whose script makes the starved colour, or cuts the worst card needing that colour when the pool has none; otherwise the worst slot goes by shrunk Δ less charges for dead-in-hand and uncast draws, and a sideboard card that did better when boarded in than the worst main-deck card swaps with it. **The search** asks `CardPoolQuery` for the deck's colours (now and then a colour its lands make) within a mana value of the card cut, drops what the 75 cannot hold, and **ranks twice** (ADR 0014): printed facts, the card's record here and Gumbel noise before scripting, then the script's answers, cards drawn and share of the opponent's observed deck after; the top `shortlistSize` are scripted and the best `trialTopK` go to trial, widening the search if nothing is playable. **Trials** (`packages/sim/src/trial.ts`) play each finalist's deck against the opponent's as a short cycle, and keep what they recorded for the deck's statistics. **The reason** reads like docs/05's example, and the evidence keeps the rates read, the slot's record and every shortlisted card's scores, support and trial. **`ScryfallPool`** (`packages/sim/src/pool.ts`) is the pool over Scryfall's projections and the resolver, scripting behind an asynchronous interface. `DeckChange` and `applyDeckChange` are in `@mtg/shared`, with two shapes — replace and swap — that keep 60/15; each match and cycle now reports what each deck showed the other. End to end over the committed fixtures, two seed decks play a cycle, the loser's deck changes (in the test run: three dead-in-hand sideboard Doom Blades out, Child of Night in on a 75% trial, with 10 of 12 shortlisted cards unscriptable), and the changed deck plays a match. **Found on the way**: a sideboard card never boarded in scored below an average card, so every early cycle would have cut the sideboard while the main deck, which plays every game, never changed; it scores as average now. The starved-colour bonus in the land score was dead — the fixing search only accepts lands that make the colour — and is gone. Of 71 rules sabotage-checked, nine survived and have tests now, among them the flood threshold, which surplus land a colour fix cuts, and a colourless-identity land that makes colours only by its script. **Deliberately left:** "on-demand scripting via the scripting worker" is the port — the worker behind it is 5.7's; legality of a change against the ban list and keeping the change, its evidence and the trial statistics are 5.5's and 5.6's; the thresholds and weights are first guesses, to be tuned on real runs.)*
- [x] 5.5 Ban list semantics and legalisation; audit trail. *(**The audit trail** is the list: `BanEvent`s — the card, ban/restrict/unban, note, who, when, and the game after which it took effect — replayed by `banListOf`; `BanRegistry` keeps them, taking an edit at any time as **pending** and putting it into effect only when the game in progress ends. **Semantics** (`@mtg/shared`): banned is none in the 75, restricted one, main and side together; `banViolations` finds a deck's excess and `applyLegalChange` refuses a change that leaves any — a second restricted copy is rejected, moving the one copy between main and side is not. **Legalisation** is `StatisticalDeckAgent.legalise`: the excess copies out, sideboard first, and each hole filled by the replacement search — the removed card's own colours and mana band first, a land by a land that makes what it made, a land when nothing of the kind can be played, no trial — each hole searched knowing what the last took. **Enforcement** is `banEnforcer`, the after-game hook `playMatch` and `runCycle` now take, both asynchronous for it: after every game it applies what is pending, reports it (docs/07's `banApplied`), and legalises each deck the list catches out, each on its own; the match goes on with the legalised decks, game 3's sideboarding starts from them, the games already played still count, and unbans change nothing. The cycle returns every forced change and the decks as they stand. End to end over the committed fixtures: a ban asked for during the first game of a cycle takes effect after it, the legalised deck plays every later game, and the other deck is untouched. **Found on the way**: a banned land was being replaced by any land in the deck's colours — twelve Mountains for twelve Forests would strand every green spell — and is now replaced by one that makes what it made. Of 29 rules sabotage-checked, two survived and have tests now: each hole seeing what the earlier ones took, and game 3's sideboarding starting from the legalised deck. **Deliberately left:** storing the trail in `ban_events`, recording a legalised deck as a generation with `cause: 'ban'`, and keeping it apart from the cycle's own change are 5.6's; the WebSocket event and the ban console are 6.2's and 6.x's.)*
- [x] 5.6 SQLite schema (drizzle migrations), checkpointing, resume after crash test, export/import, fork. *(**A run** is now a thing with a lifecycle (`packages/sim/src/run.ts`): `createRun` rolls the seed deck from the run's seed and gives it to both agents as generation 0; `driveRun` plays cycles — each one `runCycle` with the ban enforcer after every game, then the loser's change, then the cycle's record — through a **`RunStore` port** with three checkpoints: a cycle's start (with any ban that was pending between cycles applied and the decks legalised before a match is played), **every match**, and a cycle's end. Paused or stopped, it halts after the match in progress has been written. Every forced change is its own generation with `cause: 'ban'`, the loser's change one with `cause: 'change'`; `CycleRecord` in `@mtg/shared` keeps the cycle's result, play/draw records, statistics, what each deck showed, and what each trial recorded. **The SQLite store** (`apps/server/src/db`) is docs/06's tables declared once in drizzle, with `pnpm db:generate` writing the migrations the server applies on open (WAL, `synchronous=NORMAL`), every checkpoint one transaction, blobs compressed with Node's own zstd or gzip, statistics in `card_stats` rows the UI can query, and the resolver's cache and demand log as `SqliteScriptStore`; the server now opens `data/mtg.db` at boot and the image carries the migrations. **Resume replays a cycle from its stored matches** rather than from a count (**ADR 0015**, departing from docs/06): the rest of a cycle depends on the play/draw records, sideboarding records and statistics its earlier matches left, so `runCycle` takes them back as `completed`, rebuilds all three from the matches and their logs, and plays on; trials are replayed with the change from the same seeds rather than stored. **The resume test** crashes a run at every checkpoint — after a match is written, before one is, halfway through writing one (a SQLite trigger fails the second game's insert), and before a cycle is finished — closes the database file, opens it in a new store, resumes, and compares the whole run with one that never crashed, on both stores. **Fork** copies a run as of a finished cycle under a new seed with the ban trail; **export/import** is a versioned bundle with every match, logs optional, and a decklist per generation, imported under a new id and paused. A test holds the migrated database to the drizzle schema column by column, so the two cannot drift. **Found on the way**: several forced changes in one game were one generation, and are one each now; an import counted a finished cycle's matches twice, once from its record and once per match written after it. Of 16 rules sabotage-checked — among them the transaction, the play/draw and statistics rebuilt on resume, the halt on pause, the fork's cut-off and legalising at a cycle's start — none survived. **Deliberately left:** reloading `running` runs at boot, the worker processes that play the matches and the scripting worker are 5.7's; the HTTP routes for export, import and fork are phase 6's; `cards` is declared but not filled, since the sim reads the bulk JSONL and the card browser that wants the table is phase 6's; the retention setting that drops old logs is not built (ADR 0015 says why it can never touch the current cycle); a run imported without logs mid-cycle resumes without that cycle's earlier statistics; the local Docker build could not be checked here (the sandbox cannot reach the Debian mirrors), so CI's image build is the check.)*
- [ ] 5.7 Worker pool (`worker_threads`), API-process-only writes, scripting worker.

## Phase 6 — Server and UI thin slice (≈ 6–8 sessions)

- [ ] 6.1 Fastify routes for runs/cycles/decks/stats/bans/games/cards; zod DTOs; contract tests.
- [ ] 6.2 WebSocket hub with subscriptions and event batching; image proxy with cache.
- [ ] 6.3 Web app shell, runs list, new-run form with seed-deck preview.
- [ ] 6.4 Run dashboard: win-rate chart, decks with Δ chips, timeline, ban console.
- [ ] 6.5 Game viewer (replay + live) with view model, transport, ticker, hover cards.
- [ ] 6.6 Cards & coverage page.
- [ ] 6.7 Playwright smoke; Docker image built in CI.

**Milestone M1 — thin slice complete:** a run can be created with a random deck, plays cycles unattended in Docker, evolves via Scryfall replacement search, can be watched live and replayed, and reacts to a live ban. Everything in phases 1–6 is behind tests.

## Phase 7 — Widen (continuous, prioritised by the coverage page)

- [ ] 7.1 Auto-scripter grammar growth driven by the most-requested unsupported cards; target 50% then 70% of Scryfall.
- [ ] 7.2 Engine mechanics v2: double-faced/MDFC, adventures, split/fuse, sagas, morph, suspend, cascade, storm, day/night, the monarch/initiative, battles.
- [ ] 7.3 Agent strength: better evaluator terms, opponent hand inference from revealed information, tuned weights per colour combination, deeper search in tiebreaks.
- [ ] 7.4 `LLMDeckAgent` option (Claude API) with the same interface; compare against the statistical agent on identical seeds.
- [ ] 7.5 Multi-worker per run; games/sec dashboard; retention policies.
- [ ] 7.6 Multi-run comparison views; deck lineage graph; "what did the bans do" report.
- [ ] 7.7 Human-play adapter for the engine (stretch).

## Risks and mitigations

| Risk | Mitigation |
| --- | --- |
| Rules engine bugs skew evolution silently | Layered tests (09), invariant fuzzing on every push, regression fixtures, sanity ladders. |
| Auto-scripter coverage stalls on the long tail | Random-deck design tolerates gaps; effort is steered by *requested* unsupported cards, not by raw count; hand scripts remain first-class. |
| Layer system + planeswalkers in v1 is heavy | Scheduled last in phase 1 with thorough tests; the thin slice still runs if 1.9/1.11 slip, because random decks re-roll cards that need them. |
| Evaluator quality caps how meaningful "winning" is | Tuning harness; ladders; deeper search available for tiebreaks; LLM coach later. |
| Slow simulations make cycles take hours | Benchmarks in CI; worker pool; `agentLevel` and `trialTopK` knobs. |
| SQLite growth over month-long runs | Compressed logs, retention setting, export as backup. |
| Scryfall rate limits / offline homelab | Bulk data is offline; images are lazy and cached; text-only mode exists. |

## Working agreement

- Every phase ends with its tests green in CI and a short ADR (`docs/adr/NNNN-title.md`) for any decision that changed from these docs.
- Deck lists, bans, and settings are always data; never hard-code a card name in engine or agent code (card names appear only in scripts and tests).
- Determinism is non-negotiable: any new source of randomness must draw from the injected RNG.
