/**
 * The play AI (evaluator, bounded search, mulligans, sideboarding) and the
 * deck-change AI. See docs/04-agents.md.
 *
 * Nothing is implemented yet; phase 4 of docs/10-roadmap.md fills this in,
 * starting with `PlayerView` and the import-boundary test.
 */
export {};
